#!/bin/bash

# TODO: Use share space as backup space, or app data perhaps?

ls -al /
ls -al /data
ls -al /data/foundryvtt
echo Running: node "$1" --port=30000 --dataPath="$2"
node "$1" --port=30000 --dataPath="$2"
